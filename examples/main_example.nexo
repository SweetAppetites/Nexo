fc mn(){
    // 基础输出
    ehco("--- 开始执行 ---")
    
    // 调用本地函数
    an()
    
    // 调用 basic_usage.nexo 中的函数
    basic_usage.sh()

    // 演示循环结构 (While Loop)
    idx = 0
    wh (idx < 3) {
        ehco("循环计数: " + idx)
        idx = idx + 1
    }

    // 调用 mathematics.nexo 演示递归 (证明图灵完备)
    num = 5
    res = mathematics.fact(num)
    
    output = "5 的阶乘结果是: " + res
    ehco(output)
}

fc an(){
    ehco("本地函数被调用")
    fuc = {"👏","er","ff"}
}
